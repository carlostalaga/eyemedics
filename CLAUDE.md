# CLAUDE.md

This file exists for **Claude Code** (claude.ai/code). Full project instructions live in **[`AGENTS.md`](AGENTS.md)** — read that first.

**Cursor:** Coding standards are also enforced via [`.cursor/rules/coding-standards.mdc`](.cursor/rules/coding-standards.mdc) (`alwaysApply: true`). `AGENTS.md` mirrors those standards for agents that do not load `.mdc` files.
